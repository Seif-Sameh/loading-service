# Heavy RL / training dependencies — kept separate so the API image stays small.
# Install only in the training environment (Colab, Kaggle, or a dedicated dev box).
#
# torch is NOT pinned — Colab/Kaggle ship a CUDA-tuned build that we should keep.
# Pinning to a specific version downgrades it and leaves the C++ extensions out of sync,
# which crashes inside torch._dynamo on first optimizer init.
-r requirements.txt
torch>=2.5
tensorboard>=2.18
tqdm>=4.66
matplotlib>=3.9
mlflow>=2.17
